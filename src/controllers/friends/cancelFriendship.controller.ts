
import { Op, Transaction } from "sequelize"
import { fastifyController } from "../../types/global.js"

const friendshipDeletingController: fastifyController = async (request, reply) => {


}

export {
    friendshipDeletingController,
}