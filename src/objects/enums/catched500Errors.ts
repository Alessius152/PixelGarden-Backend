
/*questo enum contiene gli errori loggati da fastify.log.error nei blocchi try-catch quando si emette un errore
che porta alla restituzione del codice 500 (internal server error), che, di norma, non dovrebbe mai avvenire
se il server è scritto per bene.*/

//TO FILL
enum Flow500Error {
    
}

export {
    Flow500Error,
}
